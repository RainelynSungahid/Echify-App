{
  "$schema": "../gen/schemas/desktop-capability.json",
  "identifier": "default",
  "description": "Default permissions for the Echify App",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default",
    "shell:allow-all",
    "camera:allow-camera"
  ]
}